# Dotfiles

## Usage

```bash
# Install everything and configure sensible defaults
./install.sh

# Install Gnu Stow
brew install stow

# Install dotfiles as needed, eg. for Alacritty:
stow -t ~ alacritty
```
